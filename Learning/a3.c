#include<stdio.h>

// ? I am making a program who checks if the number is a ever number or not.
// also i am Trying to learn multiple functions usage in C
// ! Let's checkout the function invoking and usage


void main() {
    int count=0;
    int List_Of_Values[] = {0};

    printf("How Many Times Do you want to add the numbers");


} 

int INT_ADD (times) {
    int count=0;
    static int sum=0;

    for (count=1; count<=times; count++) {
        printf("\n>>> Current Sum: %d \nEnter the %d Number:");
    }
    
    return 0;
}