#include<stdio.h>

int main() {
    int number = 25;

    // ! We can't use double quotes in char type
    char star = '*'; 
    float pi = 3.141;

    printf("Value of Star is %c", star);

    return 0;
}