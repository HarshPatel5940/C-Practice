#include<stdio.h>

int main() {
    printf("\nSize of the intger datatype x is: %d", sizeof(int));
    printf("\nSize of the charactor datatype x is: %d", sizeof(char));
    printf("\nSize of the double datatype x is: %d", sizeof(double));
    printf("\nSize of the float datatype x is: %d", sizeof(float));

    return 0;
}