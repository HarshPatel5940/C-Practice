#include<stdio.h>

int main() {
    printf("\nSize of the intger datatype is: %d", sizeof(int));
    printf("\nSize of the charactor datatype is: %d", sizeof(char));
    printf("\nSize of the double datatype is: %d", sizeof(double));
    printf("\nSize of the float datatype is: %d", sizeof(float));

    return 0;
}